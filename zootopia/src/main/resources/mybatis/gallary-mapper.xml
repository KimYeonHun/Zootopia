<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="gallary">


	
	
	<insert id="add" parameterType="gallaryDto">
		INSERT INTO pet_file(
			PETSITTER_NO,PET_FILE_NAME ,PET_FILE_SIZE, PET_FILE_TYPE
		) VALUES(
			#{PETSITTER_NO}, #{PET_FILE_NAME}, #{PET_FILE_SIZE}, #{PET_FILE_TYPE} 
		)
	</insert>
	
	<select id="get" resultType="gallaryDto" parameterType="int">
		SELECT * FROM pet_file WHERE PETSITTER_NO = #{PETSITTER_NO}
	</select>
	
</mapper>